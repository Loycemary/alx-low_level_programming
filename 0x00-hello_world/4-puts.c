#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0 (Success)
 */
int main(void)
{
char putsmain[] = "\"Programming is like building a multilingual puzzle\n";

puts(putsmain);
		return (0);
}
