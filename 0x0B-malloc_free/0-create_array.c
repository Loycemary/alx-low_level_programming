#include "main.h"
#include <stdio.h>

/**
 * main - function that prints arrays of characters
 * Return: void
 *
 */

int main (void)
{ 
	char *A[];
	
	A = malloc(sizeof(cha) *4);
	 
	if(size = 0);
	A[0] = 'I'
	B[1] = 'C'
	C[2] = 'E'
	D[3] = '\0'

	printf("%c\n", A);

	return (0);
}
