#include "main.h"
#include <stdio.h>

void char *_memset(char *s, char b, unsigned int n);
{
	char b;
	unsigned int n;
	char *s;

	for (char b = 0; b < n; b++)

	printf("%d", char b);

}

int main(void)
{	unsigned int n = 10;
:	char b;

	printf("char *_memset()\n");
	return (0);
}
