#include "main.h"
#include <stdio.h>

char *_memset(char *s, char b, unsigned int n);
{
	char b;
	unsigned int i;
	char *s;

	for (i = 0; i < n; i++)

	printf("%d", i);

}

int main(void)
{	unsigned int i = 10;

	printf("char *_memset()\n");
	return (0);
}
