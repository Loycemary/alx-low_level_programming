#include "main.h"

/**
 * _memcpy -
 * Return: *dest
 *
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < 10; i++)

	{
		dest[i] = src[i]
	}

	return (dest);
}
