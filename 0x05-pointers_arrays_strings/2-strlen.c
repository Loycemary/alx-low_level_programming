#include "main.h"
#include <stdio.h>

/**
 * main - function that returns length of string
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str = "This is a string";
	int len;

	printf("%d\n", len);

	return (0);
}
